# you can set the parameters here. Code will run accordingly.

# Delay : time interval for generation of new data point
# Outliers generation probability : if outliers generation probability is 0.2 then, probability that the generated point is anomaly is 0.2
# Visualization : to display the run time figure.


DELAY = 0.5
OUTLIERS_GENERATION_PROBABILITY = 0.2
VISUALIZATION = True